{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase-1a main-window capability. Offline-only: no http permission. Ollama loopback (127.0.0.1:11434) is reached from the Rust backend, never from the webview, so no JS-side network grant is required.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
